[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "minion-code"
version = "0.1.0"
description = "A Python project depending on minion"
authors = [{name = "User", email = "user@example.com"}]
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "minionx @ file:///Users/femtozheng/python-project/minion1"
]

[project.optional-dependencies]
dev = [
    "pytest",
    "black",
    "flake8",
    "mypy"
]
tui = [
    "textual>=0.40.0",
    "rich>=13.0.0"
]
agent = [
    "openai>=1.0.0"
]

[project.scripts]
mc = "examples.textual_demo:run"
mcode = "examples.textual_demo:run"
minion-agent = "examples.minion_agent_tui:run"
minion-tui = "examples.advanced_textual_tui:run"

[tool.setuptools.packages.find]
where = ["."]